MEDIAS UPLOAD FIX — progress

Root cause: all files posted in ONE request to /api/medias → Vercel ~4.5MB body cap → multi/large upload 413 → client if(response.ok)=false → spinners hang.
Bucket products: file_size_limit=52428800 (50MB), mimes jpeg/jpg/png/webp/gif.

Fix = signed-URL direct-to-Supabase upload (bypasses body cap, honors 50MB) + client validation.

Files:
- src/lib/media-upload.ts (NEW): MEDIA_MAX_FILE_SIZE=50MB, MEDIA_ALLOWED_MIME, validateImageFile().
- src/app/api/medias/sign/route.ts (NEW): admin-gated; per-file validate + createSignedUploadUrl (service role).
- src/app/api/medias/confirm/route.ts (NEW): admin-gated; insert medias rows from {key,alt}.
- src/features/medias/uploadImages.ts (NEW): validate→sign→uploadToSignedUrl(browser)→confirm; concurrency 3; per-file status cb.
- src/features/medias/components/UploadMediaContainer.tsx: onDrop uses util; per-file spinner tile clears on done/error; toasts; dropzone accept filter; input reset via react-dropzone.
- src/features/medias/components/MultiImageDialog.tsx: "Upload từ máy tính" uses util.

Verified: E2E REST test sign 200 / upload 200 / public GET 200 / cleanup 200. tsc clean. build pass; /api/medias/sign + /confirm registered.

Learnings: supabase-js can't construct in Node20 harness (no WebSocket) — test storage via raw REST. createSignedUploadUrl POST needs a JSON body ({}). Old /api/medias POST now unused but left in place.
